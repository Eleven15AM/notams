# Software Versioning

This project uses Semantic Versioning (Major.Minor.Patch). Major versions indicate breaking API changes, minor versions add backward-compatible features, and patch versions are for bug fixes.

## .env files changes

- When building locally, modify the value of the VERSION variable in the .env file.
- When running the Github workflow, make sure that the value of .env.version is updated.